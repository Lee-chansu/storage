<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="../main.css">
</head>
<body>
    <%-include('nav.ejs') %>
    <section class="sec member">
        <div class="container text-center">
            <form action="/member" method="get">
                <!--상세페이지 조회하기-->
                <table>
                    <tr>
                        <th class="col">id</th>
                        <td class="id"><%= member.id %></td>
                    </tr>
                    <tr>
                        <th class="col">name</th>
                        <td class="name"><%= member.name %></td>
                    </tr>
                    <tr>
                        <th class="col">team</th>
                        <td class="team"><%= member.team %></td>
                    </tr>
                    <tr>
                        <th class="col">position</th>
                        <td class="position"><%= member.position %></td>
                    </tr>
                    <tr>
                        <th class="col">emailAdress</th>
                        <td class="emailAddress"><%= member.emailAddress %></td>
                    </tr>
                    <tr>
                        <th class="col">phoneNumber</th>
                        <td class="phoneNumber"><%= member.phoneNumber %></td>
                    </tr>
                    <tr>
                        <th class="col">admissionDate</th>
                        <td class="admissionDate"><%= member.admissionDate.toLocaleString("ko-kr",{dateStyle:'long'})%></td>
                    </tr>
                    <tr>
                        <th class="col">birthday</th>
                        <td class="birthday"><%= member.birthday.toLocaleString("ko-kr",{dateStyle:'long'}) %></td>
                    </tr>
                </table>
                    <!-- 사용자 정의 속성
                        형식) data-이름="값"
                        사용자가 원하는 값을 담아 노는 html속성을 만들 때
                        자바스크립트에서 해당값을 불러오고 싶을 때
                    -->
                    <p class="col">update/delete</p>
                    <div class="btn-wrap">
                        <button class="modify-page" type="button" data-id="<%= member.id %>">수정</button>
                        <button class="delete" data-id="<%= member.id %>">제거</button>
                        <button class="cansle">돌아가기</button>
                    </div>
            </form>
        </div>
    </section>

    <script>
    const modify_page = document.getElementsByClassName("modify-page")[0];
    modify_page.addEventListener('click', (e)=>{
        let id = e.target.dataset.id;
        window.location.href="/edit-page/"+id
    })

    const cansle = document.getElementsByClassName("cansle")[0];

    cansle.addEventListener('click', (e)=>{
        location.href="/member"
    })

    let del = document.getElementsByClassName('delete')[0];

    del.addEventListener('click', function(e){
        let id = e.target.dataset.id;

        //fetch 서버로 요청 날려주는 함수
        fetch('/member/'+id, {method : 'delete'})
        .then(r => r.text())
        .then(r=> {
            //성공했을 때 실행하는 결과
            window.location.href="/member" //해당 url로 이동하라.
        })
        .catch(err => {
            console.log(err)
        })
    })

    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
</body>
</html>`