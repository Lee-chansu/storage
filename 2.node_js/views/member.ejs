<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="main.css">
</head>
<body>

    <%- include('nav.ejs') %>

    <div class="sec container">
        <div class="title">
            <h1>Member</h1>
        </div>
        <div class="row member text-center">
            <table>
                <tr>
                    <th class="col-1 no">No</th>
                    <th class="col-1">이메일</th>
                    <th class="col-1">나이</th>
                    <th class="col-1">성별</th>
                    <th class="col-1">키</th>
                    <th class="col-1">몸무게</th>
                    <th class="col-1">생일</th>
                    <th class="col-1">가입날짜</th>
                    <th class="col-4">주소</th>
                </tr>
            <% for(let i of person) { %>
            <tr>
                <td class="no"><%= i.id %> </td>
                <td class="email"><%= i.email %> </td>
                <td class="age"><%= i.age %> </td>
                <td class="gender"><%= (i.gender == 'm') ? '남':'여' %></td>
                <td class="height"><%= i.height %></td>
                <td class="weight"><%= i.weight %></td>
                <td class="birthday"><%= i.birthday %></td>
                <td class="sign_up_day"><%= i.sign_up_day %></td>
                <td class="address"><%= i.address %></td>
            </tr>
            <%} %>
            
            </table>
        </div>  
    </div>  

    
</body>
</html>