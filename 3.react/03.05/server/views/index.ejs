<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/blog.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
</head>
  
<header class="header">
    <div class="container">
      <div class="row">
        <div class="col login-info" data-login-state="<%= loginUserName %>">
          
        </div>
      </div>
    </div>
  </header>

  <section class="blog-post sec text-center">
    <form action="/add" method="post" id="post-form">
      <div class="container">
        <h2 class="title">✍ Memo Write!</h2>
        <%- include('write.ejs') %>
    </div>
  </form>
</section>

<section class="blog-list sec" id="blog-list">
  <div class="container" style="background-color: transparent;">
    <div>
      <a href="/page/1" >전체 검색</a>
      <% if(loginUserId) { %>
      <a href="/page/1?userid=<%= loginUserId %>"> / 내 post 검색</a>
      <% } %>
    </div>
  </div>
  <div class="container">  
    <h2 class="title  text-center">📜 Memo List!</h2>
    <!-- 로그인 user와 일치하는 게시글에 클래스 추가 -->
    <% for(let i = 0; i < blog.length ; i++) { %>  
      <% if(loginUserName == blog[i].User.username) { %>
      <div class="item row active">
      <% } else { %>
      <div class="item row">
      <% } %>
      <div class="row m-0 p-0">
        <div class="user-info col d-flex justify-content-between">
          <div class="name">
            <i class="bi bi-person-fill"></i>
            <span id="login-user"><%=blog[i].User.username %></span>
          </div>
          <% if (blog[i].User.username && blog[i].User.username == loginUserName ) { %>
          <div class="btn-wrap">
            <a class="edit" data-id="<%=blog[i].id %>">✏</a>
            <a class="del" data-id="<%=blog[i].id %>">🗑</a>
          </div>
          <% } %>
        </div>
      </div>
      <div class="row m-0 p-0">
        <div class="user-post col">
          <div class="d-flex align-items-center">
            <label for="blogTitle" class="col-2">제목</label>
            <input type="text" name="title" class="blog-title" readonly value="<%= blog[i].title %>">
          </div>
          <div class="d-flex align-items-center">
            <label for="blogContent" class="col-2">내용</label>
            <input type="text" name="Content" class="blog-content" readonly value="<%= blog[i].content %>">
          </div>
        </div>
      </div>
    </div>
    <% } %>

    <div class="row pagination d-flex justify-content-center">
      <% for(let page = 1; page <= totalPage ; page++) { %>  
        <% if(queryUserId) { %>
          <a href="<%= page %>?userid=<%= loginUserId %>"><%= page %></a>
          <% } else { %>
          <a href="<%= page %>"><%= page %></a>
        <% } %>
      <% } %>
    </div>
  </div>
</section>  

<script src="/js/post.js"></script>

</body>
</html>